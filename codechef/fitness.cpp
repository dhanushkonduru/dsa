#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;
    
    for (int i = 0; i < T; ++i) {
        int X;
        cin >> X;
        int total_distance = 10 * X;
        cout << total_distance << endl;
    }
    return 0;
}
